function main(var bootParam)
{
	// First, setup engine variables
	VAR_TIMER_NEXT = 1;
	VAR_MAINMENU_KEY = 319;
	VAR_RESTART_KEY = 322;
	VAR_VERB_SCRIPT = Input_handler;
	VAR_SENTENCE_SCRIPT = Input_sentenceHandler;

	// Load the logo screen
	Room_setScreen(0, 200);
	Room_load(logoRoom);
}
